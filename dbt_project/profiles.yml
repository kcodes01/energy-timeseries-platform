dbt_project:
  target: dev
  outputs:
    dev:
      type: clickhouse
      schema: energy_marts
      host: "{{ env_var('DBT_CLICKHOUSE_HOST', 'localhost') }}"
      port: 8123
      user: clickhouse
      password: clickhouse
      secure: false
      verify: false
